{
  "name": "bustabot",
  "description": "Telegram Chat Bot.",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "author": "Ricardo Bustamante",
  "repository": {
    "type": "git",
    "url": "https://github.com/RicardoBusta/BustaBotNode.git"
  },
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "deploy": "gcloud app deploy",
    "start": "node app.js",
    "system-test": "repo-tools test app",
    "test": "npm run system-test",
    "e2e-test": "repo-tools test deploy"
  },
  "dependencies": {
    "express": "^4.16.3",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "@google-cloud/nodejs-repo-tools": "^3.0.0"
  },
  "cloud-repo-tools": {
    "test": {
      "app": {
        "msg": "Hello, world!"
      }
    },
    "requiresKeyFile": true,
    "requiresProjectId": true
  }
}
